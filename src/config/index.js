const URL_BACKEND = window.location.hostname.includes('localhost')
  ? 'http://localhost:8080'
  : 'http://localhost:8080';

const URL_ALURA= 'https://www.alura.com.br';
export default {
  URL_BACKEND,
  URL_ALURA,
};

